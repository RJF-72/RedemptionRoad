{
  "name": "sota-synth-smoke",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serve": "http-server -p 5173 -c-1 .",
    "test": "playwright test -c playwright.config.js",
    "test:headed": "playwright test --headed",
    "install:browsers": "playwright install --with-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.2",
    "http-server": "^14.1.1"
  }
}
